@model List<ClientMDA.ViewModels.CQANDAViewModel>
@{
    ViewData["Title"] = "Default"; @*退票問題*@
}

<div id="Qbox">
    <div class="accordion" id="accordionExample">
        @foreach (var q in Model.First().退票問題refundQa)
        {
            <div class="card" id="Qcard">
                <div class="card-header" id='heading_@q.退票問題refundQaId'>
                    <h2 class="mb-0">
                        <button class="btn btn-link collapsed" type="button" data-toggle="collapse" data-target="#collapse_@q.退票問題refundQaId " aria-expanded="false" aria-controls="collapse_@q.退票問題refundQaId" id="Qbtn">@q.問題question</button>
                    </h2>
                </div>

                <div id="collapse_@q.退票問題refundQaId" class="collapse" aria-labelledby="heading_@q.退票問題refundQaId" data-parent="#accordionExample">
                    <div class="card-body">@Html.Raw(@q.答案answer)</div>
                </div>
            </div>
        }

    </div>
</div>